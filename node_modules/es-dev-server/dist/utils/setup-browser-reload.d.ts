import { FSWatcher } from 'chokidar';
export interface SetupBrowserReloadConfig {
    fileWatcher: FSWatcher;
    watchDebounce: number;
}
export declare function setupBrowserReload(cfg: SetupBrowserReloadConfig): void;
